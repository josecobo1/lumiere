import { WhereWatchCountryPipe } from './where-watch-country.pipe';

describe('WhereWatchCountryPipe', () => {
  it('create an instance', () => {
    const pipe = new WhereWatchCountryPipe();
    expect(pipe).toBeTruthy();
  });
});
