import { PosterImgPipe } from './poster-img.pipe';

describe('PosterImgPipe', () => {
  it('create an instance', () => {
    const pipe = new PosterImgPipe();
    expect(pipe).toBeTruthy();
  });
});
